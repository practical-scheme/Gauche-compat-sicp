#!/usr/bin/env gosh
;; Configuring Gauche-compat-sicp
;; Run ./configure (or gosh ./configure) to generate Makefiles.

(use gauche.configure)
(cf-init-gauche-extension)
(cf-output-default)

;; Local variables:
;; mode: scheme
;; end:
